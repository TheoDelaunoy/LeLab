<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage Title="Liste"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="lelab.Views.ListPage">
    <ContentPage.Content>
        <ListView x:Name="maListeView" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="Center" Margin="15,0,15,0">
                            <Label Text="{Binding Nom}" HorizontalOptions="FillAndExpand" TextColor="Black"/>
                            <Label Text="{Binding Prix}" HorizontalTextAlignment="End" FontAttributes="Bold" TextColor="Black"/>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>

